# Atmospheric Ink 1.3.1: selected art direction

The owner selected this version on 2026-09-15 and asked to keep it locked.

- Exact prior slice: `atmospheric-ink-v1.3.1.zip` (32 files).
- Source revision: `060c78c806f2ad17fed76a4951797b553d3beb09`.
- Git tag: `art/atmospheric-ink-v1.3.1`.
- Recipe, approval and file identities: `atmospheric-ink-v1.3.1.json`.

Extract the ZIP into an empty directory, serve it with any static HTTP server,
and open `style-playground.html?style=v5`. It preserves the approved scene and
original render dependencies, including the historical world-projected pigment.
The manifest's approval is current; the frozen recipe's old review label is part
of the exact snapshot.

The expanded `../atmospheric-arena.html` keeps the same art parameters with
corrected mesh-attached pigment, FPS combat, extra environments and temporary
graphics overrides. Its Reset to 1.3.1 restores the exact parameter baseline.
Do not replace the ZIP or change its declared identities when iterating further.
Add a separately named version and record the owner's decision. The original
Hard Cel archive remains another preserved candidate.
