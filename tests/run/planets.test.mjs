import {test} from 'node:test';import assert from 'node:assert/strict';
import {campaignRoute,planetDefinition,MILESTONES} from '../../js/run/planets.js';
test('99 route nodes are legal, distinct and have bounded difficulty',()=>{const route=campaignRoute(12345);assert.equal(route.length,99);assert.equal(new Set(route.map(p=>p.name)).size,99);assert.equal(new Set(route.map(p=>`${p.seed}:${p.terrain}`)).size,99);assert.equal(new Set(route.map(p=>`${p.terrain}:${p.pressure}`)).size,5);assert.ok(route.every(p=>p.terrain==='varied'));for(const [i,p]of route.entries()){assert.equal(p.index,i+1);assert.ok(p.seed>0&&p.seed<=0xffffffff);assert.ok(p.enemyHealth>=1&&p.enemyHealth<=1.35);assert.ok(p.brief.length>20);}assert.deepEqual(campaignRoute(12345),route);assert.notDeepEqual(campaignRoute(12346),route);});
test('era and final-planet boundaries remain explicit',()=>{for(const [index,era]of [[1,'ancient'],[33,'ancient'],[34,'technological'],[66,'technological'],[67,'empowered'],[98,'empowered'],[99,'empowered']])assert.equal(planetDefinition(index,42).era,era);for(const index of [0,100,-1,1.5])assert.equal(planetDefinition(index,42),null);assert.equal(planetDefinition(1,NaN),null);assert.equal(planetDefinition(99,42).boss.name,'The Returning Crown');});
test('the mixed-landscape opening and authored milestone tells remain identifiable',()=>{const route=campaignRoute(12345);assert.deepEqual(route.slice(0,3).map(p=>[p.name,p.terrain,p.seed,p.enemyHealth]),[['Hearthwild','varied',12345,1],['Riftshore','varied',117074,1],['Crownfall','varied',221803,1.04]]);assert.equal(Object.keys(MILESTONES).length,13);for(const p of route.filter(p=>p.boss)){assert.ok(['sweep','lance','bastion'].includes(p.boss.style));assert.ok(Number.isInteger(p.boss.tint));}});
