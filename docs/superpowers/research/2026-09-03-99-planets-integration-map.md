> **HISTORICAL RECORD.** This file records what was intended when it was
> written. It was not maintained afterwards and several details have since
> changed in the code. Treat it as background on the reasoning, not as a
> description of the current game. For that see [README](../../../README.md),
> [CLAUDE.md](../../../CLAUDE.md) and
> [docs/ARCHITECTURE.md](../../ARCHITECTURE.md).

# 99 Planets — integration map (WORLDHEART)

<!-- Six parallel readers over game.js, towers.js, enemies.js, waves.js+main.js,
     ui.js+style.css and config.js+camera.js+world.js+nav.js, 2026-09-03.
     Several findings were verified in a live browser or measured, not estimated.
     Written for plan 1b (wiring the pure run core into the Three.js game). -->

## Integration point index

```

### game.js
  PLACEMENT COMMIT — the full _tryPlace path: gold deduction game.js:380
  PLACEMENT VALIDATION — _validate, all 8 rules, including t game.js:348
  GOLD EARNED (kills) + LIVES LOST (leaks) + the ONLY defeat game.js:233
  GOLD EARNED (wave clear + early call) — inside WaveDirecto waves.js:91
  TOWER TIER — where tier lives and where the cap is enforce game.js:437
  TOWER BASE COST reads — every site a '-10% tower cost' mod towers.js:11
  STATE TRANSITIONS — every assignment to game.state, and ev ui.js:388
  THE SIM STEP — where a pure core's update(dt) would be dri main.js:388
  BOOT WIRING — where a modes/ninetynine.js shell would be c main.js:255
  SEEDED RNG already exists and is the injectable the pure c noise.js:5

### towers.js
  THE PRIMARY MULTIPLIER SEAM — override the stats getter so towers.js:369
  Fallback / additional seam if the stats getter is not over towers.js:467
  Fire-rate multiplier — TWO sites, plus tesla which has no  towers.js:462
  Range multiplier — TWO independent reads, cryo bypasses _a towers.js:386
  Crit — the only roll in the codebase, and it is unseeded   towers.js:469
  Chain to +1 target — the working reference implementation  towers.js:524
  Pierce — nothing exists; the bolt pool object must gain fi towers.js:645
  Global tower tier cap (waves 10 and 12) — THREE hardcoded  towers.js:373
  Per-wave tower unlock (waves 2,4,6,8) — the shop has no un ui.js:499
  Locked/unlocked shop thumbnails                            ui.js:284
  Economy / gold-per-kill for run-scoped powers              game.js:234
  The per-tower update entry the shell must drive            towers.js:426

### enemies.js
  ARMOUR (flat reduction) + REGENERATING SHIELD hook: the on enemies.js:207
  DEATH: where an enemy dies (kill-gold multiplier and 'mite enemies.js:227
  GOLD AWARD: not in enemies.js - gold is awarded only when  game.js:234
  GOLD AWARD, upstream: the wrapper that detects a tower kil towers.js:705
  ENEMY SPEED read per frame (speed evolution hook)          enemies.js:271
  HP initialised from hpScale (evolution / wave scaling hook enemies.js:60
  REACHING THE HEART (leak). Two branches: space vs surface  enemies.js:317
  ENEMY COLOUR / MATERIAL (the visual evolution tell)        enemies.js:118
  Existing precedent for spawning children from a damaged en enemies.js:217
  Sim tick order the mode shell must respect                 main.js:396
  Frontier (spherical cap theta) already exists as a static  config.js:32
  Directories the feature assumes                            js:1

### waves.js
  THE WAVE-CLEARED POINT — the single place a wave is declar waves.js:152
  Only per-wave HP scale application                         waves.js:111
  Only enemy spawn call in the whole director                waves.js:147
  main.js BOOT SEQUENCE — the whole ordered construction, th main.js:209
  HUD claims three wave callbacks during construction at mai ui.js:369
  stepFrame — the single per-frame tick; every mode update m main.js:388
  The WH debug object — complete property list               main.js:784
  CONFIG.map / CONFIG.mapKey selection — where a new mode wo config.js:83
  Frontier theta (spherical cap) — the three places a cap an main.js:225
  Tower unlock waves 2/4/6/8 — the two gates that decide whe game.js:281
  Global tower tier cap raised on waves 10 and 12 — the sing game.js:437

### style.css (394 lines)
  CRITICAL: the .overlay hit-testing gate. This is the exact style.css:278
  CRITICAL companion: the #hud pointer-events cascade that m style.css:83
  _buildCards: how shop cards are created, labelled, keyed a ui.js:179
  How a card is greyed out when unaffordable. Locked-tower s ui.js:496
  The visual definition of the greyed-out (locked) card stat style.css:220
  Overlay 1 of 2: the title overlay, full HTML pattern a new ui.js:122
  Overlay 2 of 2: the end (defeat/victory) overlay markup, a ui.js:143
  The exact show/hide-a-modal pattern, including the pause c ui.js:404
  Overlay 3: the settings popover (a non-modal, display-togg style.css:343
  Overlay 4: toasts. How they are raised and torn down.      ui.js:448
  The post-wave hook where a draft must be raised.           ui.js:369
  The clickable-card-inside-an-overlay prior art (map cards) ui.js:201
  reflectPause and its external callers.                     ui.js:438
  Per-frame wave-label logic hard-wired to CONFIG.waves.coun ui.js:549
  HUD construction site and the WH debug surface.            main.js:276
  The design tokens and visual language a new overlay must m style.css:3
  The single-file build script has a FLAT module list and a  build.mjs:13

### config.js
  MAPS registry - the full literal, all four entries, every  config.js:23
  mapKey selection - query string then localStorage then def config.js:83
  CONFIG - every field                                       config.js:92
  rig.confine - declaration and exact shape                  camera.js:69
  rig.confine - the ONLY place it is SET                     main.js:225
  rig.confine - every read site                              camera.js:658
  rig.camScale / distMin / distMax - how fieldTheta feeds th camera.js:226
  buildFieldWall - full builder, and why theta CAN be resize world.js:1403
  addFieldWall / addCloudDeck / addPortal on World, and the  world.js:1587
  Call site for the wall and cloud deck - gated on mode ===  main.js:243
  Outside-the-cap haze - BAKED INTO TERRAIN VERTEX COLORS, n world.js:381
  Cloud deck - theta IS a live uniform (the model to copy)   world.js:654
  nav.js cap pruning during subdivision - where fieldTheta p nav.js:57
  nav._buildGraph - every array is reallocated, so the graph nav.js:267
  Where nav.fieldCenter is set, and the cap-center seed sear nav.js:168
  Portal placement and the index coupling that makes runtime main.js:247
  portalCount / portalWakes - the existing per-wave unlock m waves.js:15
  setBattlefield - the single mutation point for the cap     world.js:32
  Decor scatter is cap-bounded at build time                 world.js:1143
  Directories for the new code do not exist yet              js:1```

## Gotchas and patterns

```

### game.js
  ! THERE IS NO 'victory' STATE. Grep across js/ finds exactly three writes to game.state: 'title' (game.js:177), 'defeat' (game.js:259), 'playing' (ui.js:391). Victory is purely a UI overlay via waves.onVictory -> ui.js:379 showEnd(true), and ui.js:418 sets `this.game.paused = true;` while state STAYS 'playing'. Any plan that says 'game.state transitions to victory' is describing code that does not exist.
  ! THE KEYDOWN LISTENER IS PERMANENT AND UNSCOPED. game.js:223 `addEventListener('keydown', (e) => {...})` binds to window with no reference kept and no removal path. ui.js:363 adds a second one. Constructing a second Game (which a roguelite 'next planet' flow implies) DOUBLE-BINDS every hotkey: Digit1-5, Escape, KeyU, KeyX would fire on both instances. There is no dispose()/teardown on Game at all. Today this is masked because restart is a full page reload — ui.js:261-263 `_reboot() { location.href = location.pathname; }`.
  ! Game reaches THROUGH three.js object graphs to do gameplay logic. game.js:352 `this.world.heart.group.position`, :354 `p.group.position`, :253-254 `this.world.setHeartHealth(...)`, :246 `this.rig.addTrauma(0.5)`, :396 `this.rig.addTrauma(0.06)`. Distances are measured in THREE.Vector3 world space against mesh transforms. A pure core cannot own placement validation without either the shell pre-resolving these to plain numbers, or the core speaking only in nav node indices (nav.js exposes nodePos/nodeDir at :754/:757 and nearestWalkableNode at :368).
  ! WaveDirector MUTATES game.gold DIRECTLY. waves.js:70 `this.game = game;` then waves.js:94 `this.game.gold += bonus;` and waves.js:154 `this.game.gold += reward;`. There are FIVE separate writers to game.gold across three files (game.js x4, waves.js x2, main.js:798 WH.give). A pure core cannot be the authority on gold until every one of those is routed through it.
  ! TOWER_TYPES and ENEMY_TYPES ARE SHARED MUTABLE SINGLETONS and the codebase already mutates one at load: towers.js:65-69 `if (CONFIG.map.mode === 'space') { TOWER_TYPES.mortar.air = true; TOWER_TYPES.mortar.desc = '...'; }`. Instances hold references (`this.def = TOWER_TYPES[typeKey]` towers.js:331; `this.type = type` enemies.js:63), so a power that mutates a type object retroactively changes every existing unit AND leaks across runs within one page load. Powers must live in a separate modifier object read at the point of use, exactly as the design says.
  ! SEVEN Math.random() CALLS SIT INSIDE THE SIMULATION and will break determinism/replay: waves.js:124 `t: 1.2 + i * g.gap + Math.random() * 0.3` (spawn timing — this one changes which enemies exist when), towers.js:469 `if (st.crit && Math.random() < st.crit)` (crit rolls — changes damage), towers.js:742 (shell scatter), enemies.js:194 (spawn jitter on the direction vector). Cosmetic-only ones: enemies.js:80 (phase), towers.js:344 (yaw), towers.js:601 and :854 (spark rate). The seeded generator already exists — noise.js:5 mulberry32 — but nothing in the sim uses it.
  ! CONFIG IS READ AT MODULE LOAD AND BAKED IN. game.js:19 `const PATH_RAISE = CONFIG.map.mode === 'space' ? 2.6 : 0.24;` evaluates once at import. config.js:93 reads localStorage and URL params for the seed. CONFIG.economy.startLives (config.js:130) is used as the heart-health DENOMINATOR at game.js:254 rather than the run's actual starting lives — a 'start with +5 lives' power would make the heart bar read >100% and never reach empty correctly. CONFIG.waves.count = 30 (config.js:122) is checked at waves.js:156 for victory and at ui.js:550/:555 for the 'ENDLESS' label.
  ! THE BUILD CARD COST TEXT IS BAKED INTO innerHTML ONCE. ui.js:190 `<div class="build-cost">${def.cost}</div>` runs inside HUD._build at construction. refresh() (ui.js:484) only toggles the 'locked' class (ui.js:499) and never rewrites the number. A live cost modifier will show the wrong price on the build bar unless the card text is re-rendered. Same class of bug for the ghost: game.js:306 `buildTowerVisual(typeKey, 0)` and game.js:324 `def.tiers[0].range` are hard-coded to tier 0, so a 'towers start at tier 1' power would show the wrong ghost and the wrong range ring.
  ! THE TIER CAP OF 2 IS HARD-CODED IN THREE PLACES that do not know about each other: game.js:439 `if (!t || t.tier >= 2) return;`, towers.js:373 `if (this.tier >= 2) return false;`, ui.js:530 `if (t.tier < 2) {`. Also every tiers array is EXACTLY 3 entries long (towers.js:16-19, 26-29, 36-39, 46-49, 56-59) and towers.js:369 `get stats() { return this.def.tiers[this.tier]; }` does no bounds check — a cap raised above 2 returns undefined and the next `st.range` read throws. The waves-10/12 'cap raise' must therefore be a cap that STARTS below 2 and climbs to it.
  ! game.update(dt) IS CALLED WITH UNSCALED dt OUTSIDE THE SIM GATE. main.js:403 `if (game) game.update(dt);` sits after the `if (simDt > 0) { ... }` block (main.js:398-402), so it advances while paused and ignores game.speed. Anything the pure core owns must NOT be driven from Game.update; it belongs beside waves.update(simDt) at main.js:399.
  ! towerMgr.onKillReward FIRES WITH TWO ARGS BUT THE HANDLER TAKES ONE. towers.js:715 `if (this.onKillReward) this.onKillReward(enemy, tower);` vs game.js:234 `this.towerMgr.onKillReward = (enemy) => {`. The `tower` argument is silently discarded — free to use, and worth knowing before a plan claims per-tower kill credit needs new plumbing.
  ! enemies.onKill IS DECLARED AND FIRED BUT NEVER ASSIGNED. enemies.js:100 `this.onKill = null;`, fired at enemies.js:229. Nothing in the repo sets it. It is an unused, ready-made subscription point for a pure core that wants kill events independent of the reward/FX path.
  ! THE FRONTIER MECHANIC PARTIALLY EXISTS ALREADY, at world-build time only. CONFIG.map.fieldTheta (config.js:32/45/63/76 — null, null, 0.28, 0.5) drives nav.js:121, nav.js:191 `_pickCapCenter(rng, relax, theta)`, nav.js:229-247 (cap-pruned icosphere: nodes outside `dot >= cos(capTheta + 0.02)` are DELETED from the graph, not just marked), camera.js:228 (rig.confine), world.js:61, main.js:226/229/244/245 (field wall + cloud deck meshes). Because the cap PRUNES the nav graph at build time, a theta that GROWS each wave from 0.12 to 0.52 cannot reuse this path — the graph must be built once at the largest theta (0.52) and the frontier enforced as a per-placement dot-product test plus a moving visual wall. main.js:244 `world.addFieldWall(nav.fieldCenter, CONFIG.map.fieldTheta)` builds a static mesh; there is no setter to resize it.
  ! MINOR: game.js:3 imports `clamp` from noise.js and never uses it. game.js:193 `this._pathT` is assigned at :193 and :287 and never read. ui.js:385 `_waveTag(n)` has a dead first line `const comp = this.waves.constructor === Object ? [] : [];`. Harmless, but do not treat them as live wiring.
  ~ Callback-slot wiring, not an event bus. Every cross-module signal is a nullable field assigned by the consumer and null-checked at the call site: `this.onHudChange = null;  // ui hook` (game.js:202) then `_hud() { if (this.onHudChange) this.onHudChange(); }` (game.js:468). Same shape at towers.js:629/715, enemies.js:99-102/229/322, waves.js:78-82. A pure core emitting events should expose the same slot shape (this.onEvent = null) so the shell wires it identically, or return an event array from update(dt) — but do NOT introduce an EventEmitter, nothing in the repo has one.
  ~ Context object splat in the constructor: `Object.assign(this, ctx); // scene, rig, world, nav, enemies, towerMgr, fx` (game.js:172). HUD uses destructured-params instead (ui.js:2 `constructor({ game, waves, world, nav, rig, renderer, audio })`). Prefer HUD's explicit destructuring for js/modes/ninetynine.js — the splat makes it impossible to see what a class depends on.
  ~ Module-scope scratch vectors to avoid per-frame allocation: game.js:10-15 declares `_v, _v2, _n, _hit, _e1, _e2` and every method borrows them. towers.js, enemies.js, nav.js do the same. The pure core must NOT do this with THREE.Vector3 — use plain {x,y,z} or flat Float64Array, or better, keep vectors out of the core entirely and let the core speak in node indices and angles.
  ~ Typed arrays + generation-stamped scratch for hot graph work: nav.js:715 blockNodes writes into `this.block` (a per-node Int array keyed by towerId), nav.js:721 unblockNodes scans all n nodes clearing that id, and nav.js:688 `const mark = this._scratch();` with `const g = this._gen;` avoids allocating a visited buffer per hover. Match this if the core ever owns spatial state.
  ~ Cosmetic state is derived, never authoritative — stated as a rule in the towers.js header comment (towers.js:8-9): "Cosmetic animation reads sim state, never the other way around", and again at enemies.js:8-9. The core/shell split is the same rule taken one level further.
  ~ Data tables as exported module constants read by index: TOWER_TYPES (towers.js:11), ENEMY_TYPES (enemies.js:11), CONFIG (config.js). Instances hold a `.def` / `.type` REFERENCE into the shared table (towers.js:331 `this.def = TOWER_TYPES[typeKey];`, enemies.js:63 `this.type = type;`). Never mutate through those references — see gotchas.
  ~ Pooling with an `active` flag rather than allocation: enemies.js `this.pool = []` / `this.active = []` (enemies.js:96-97), towers.js bolts/shells arrays with `{ active: false, ... }` (towers.js:646, 661). A 15-wave run does not need this, but the shell should reuse enemies.spawn's pool rather than constructing enemies itself.
  ~ Guard-and-return validation returning a tagged object, not exceptions: `_validate` (game.js:348) returns `{ ok, reason }` and the caller maps reason -> message via a literal object (game.js:385-394). Frontier rejection should extend that same map rather than throw.

### towers.js
  ! Tower.stats returns the SHARED module-level tier object by reference (line 369: `return this.def.tiers[this.tier];`). Writing to it mutates every tower of that type, every future tower, and — since ES module state persists — every restart within the page session. towers.js:67-68 already mutates TOWER_TYPES.mortar at import time, so nothing in the codebase treats these literals as immutable. A run modifier MUST return a derived copy.
  ! FIVE unseeded Math.random() calls in towers.js: line 344 (initial turret yaw, cosmetic), line 469 (THE CRIT ROLL — gameplay-critical), line 601 (helios spark, cosmetic), line 742 (mortar shell scatter — gameplay-relevant, it moves the blast centre by up to 0.35 units in each axis), line 854 (shell smoke, cosmetic). A seeded-RNG core must intercept at least 469 and 742.
  ! MORTAR DAMAGE IS RESOLVED LATE AND BY REFERENCE. fireShell stores `s.st = st` at line 747 — the live tier object — and TowerManager.update reads `s.st.dmg` and `s.st.aoe` at 832/834/840, up to 1.5 seconds later (dur is clamped to 0.7-1.5 at line 745). If the tower is upgraded or a power changes mid-flight, in-flight shells retroactively use the NEW values. Bolts do the opposite: b.dmg is frozen at fire time (line 726). The two projectile types disagree about when damage is decided.
  ! Tower ids come from a module-level `let towerSeq = 1;` at line 325 that is NEVER reset. TowerManager.beams is keyed by tower.id (line 780) and nav.blockNodes is keyed by tower.id (game.js:402). A multi-player or multi-run model that expects per-run id spaces will not get one.
  ! `TOWER_SCALE` is imported in ui.js (line 3) but never used in makeThumbnails() — thumbnails render UNSCALED geometry framed by a fixed camera at ui.js:270-272 (`cam.position.set(2.6, 2.3, 2.6); cam.lookAt(0, 0.85, 0);`). If a power changes tower size, thumbnails will not reflect it.
  ! makeThumbnails() (ui.js:266-310) is called exactly once, from main.js:277. It creates and disposes its own 112px WebGLRenderTarget (268, 309). Re-running it per wave for unlock states means either re-entering that whole function or caching both variants on the first pass.
  ! The `.locked` class is PURELY COSMETIC. css/style.css:220-221 sets opacity/saturate/cursor but the click handler at ui.js:197 has no guard and the Digit1-5 hotkeys at game.js:225-226 have no guard. A 'locked' tower today is still selectable into build mode; only the gold check at game.js:368 stops the actual placement. Wave-gated unlocks need a real gate, and toggleBuild (game.js:281) is the single cheapest place to put it.
  ! TowerManager.place() applies castShadow/receiveShadow by traversing tower.holder (682-688), but Tower._buildVisual() (351-367) rebuilds this.group on every upgrade WITHOUT re-running that traverse. Upgraded towers therefore lose their shadow flags. Any tier-cap change that produces more upgrades makes this more visible.
  ! Line 512 `MAT.energy.emissiveIntensity = 1.5;` is a write to a SHARED material from inside a per-tower update, executed every frame by every tesla. Its own comment concedes the problem: `// shared; per-tower glow via tip scale`. Do not add per-tower material writes here.
  ! Helios uses `{ trueDamage: true }` at line 596, which makes enemies.js:210 skip the armour subtraction entirely. Tesla hardcodes `armorPierce: 3` (527) and mortar hardcodes `armorPierce: 99` (840) — effectively also true damage. Only BOLT actually respects armour (line 808 passes no pierce). The wave-3 armour evolution will therefore only affect bolt towers unless these literals move into the tier data or the modifier.
  ! Mortar blast skips flyers at impact — line 836 `if (!e.active || e.dead || e.type.flying) continue;` — even though towers.js:67 sets `TOWER_TYPES.mortar.air = true` in space mode, which lets _acquire (398) target a flyer. In space mode a mortar can aim at a flyer and then deal it zero damage.
  ! enemies.js damage() has a floor: line 212 `dmg = Math.max(1, amount - armor);` — non-true damage can never be reduced below 1 per hit. A damage-reduction interaction cannot zero anything out. It also silently applies a 1.12x brittle bonus at line 214 that no tower knows about.
  ! Dead code in _acquire, lines 390-391: `const d2 = this.target.renderPos ? 0 : 0;` and `_v.copy(this.target.dir).multiplyScalar(this.target.type.flying ? 0 : 0);`. Both evaluate to 0 unconditionally and both results are discarded. Line 391 does clobber the shared scratch _v as a side effect. Line 527's `const dealt =` is likewise assigned and unused.
  ! There are TWO copies of every file: js/ and v2/js/. v2/js/towers.js is byte-identical at every line number cited here (v2/js/towers.js:11, :71, :317, :319, :620 all match). Per the recent commits ('Refresh /v2 with...', 'Bring the root Pages deployment up to the current build'), v2/ is a published deployment mirror. Any change to js/towers.js must be mirrored or v2/ will drift.
  ! Neither js/run/ nor js/modes/ exists. towers.js currently imports three (line 1), CONFIG/PALETTE/REDUCED_MOTION from ./config.js (2), clamp/lerp from ./noise.js (3), and R/orientOnSurface from ./world.js (4) — so nothing in towers.js can be imported by a pure core without a split. js/noise.js (127 lines) is the only existing dependency-free-looking module worth checking as a shared utility for the core.
  ~ Data lives in a frozen-by-convention literal at the top of the module, behaviour reads it through a getter. TOWER_TYPES (11-62) is pure data; Tower.stats (369) is the single accessor. Follow this: the run modifier should be pure data, and towers should reach it only through an accessor.
  ~ `st` is resolved ONCE per tick at line 433 and passed as the 4th positional arg to all five _updateX handlers (438-442). Every handler signature is `_updateX(dt, enemies, fx, st)`. Preserve that shape — it is why one getter override covers all five species.
  ~ Effects and audio are optional and always guarded: `this.manager.audio?.play('shot')` (472), `this.audio?.play('explosion')` (833), `if (this.onKillReward) this.onKillReward(...)` (715). Events out of the sim already use nullable callbacks (onKillReward at 629), which is the existing precedent for the 'emit events rather than call the renderer' rule.
  ~ Object pools with a hard cap and silent drop: MAX_BOLTS=80 (617), MAX_SHELLS=24 (618), `const b = this.bolts.find((x) => !x.active); if (!b) return;` (721-722). New projectile behaviours should reuse these pools, not allocate.
  ~ Module-level scratch vectors are reused everywhere: _v, _v2, _v3, _q, _q2, _m4, _s declared at 116-122. Functions freely clobber them across call boundaries (e.g. _dist2 at 408 writes _v3 while _updateBolt holds _v2). Any new helper must assume these are volatile.
  ~ Comments in this codebase explain WHY a non-obvious choice was made, at the point of the choice — see 316 (TOWER_SCALE rationale), 64-65 (space-mode mortar), 678-681 (why shadow flags live in place() not buildTowerVisual), 342-343 in game.js (footprint scaling). Match that register.
  ~ Multi-projectile is already modelled declaratively: `const shells = st.shells || 1;` then a stagger loop (487-490). Use `|| 1` / `|| 0` defaults for optional tier fields rather than adding the field to every tier.
  ~ Tier changes geometry, not just numbers — every builder branches on `tier >= 1` / `tier >= 2` (166, 186, 207, 238, 295) and Tower._buildVisual (351-367) tears down and rebuilds on upgrade. A new tier or a power-granted visual must go through _buildVisual, which also resets buildT to 0 (366) and replays the rise animation.

### enemies.js
  ! enemies.onKill is dead wiring. It is declared (enemies.js:100) and fired (enemies.js:229) but assigned NOWHERE in js/. Gold and score come only from TowerManager.onKillReward (towers.js:715 -> game.js:234-242), which fires only inside TowerManager.applyDamage. Consequence for 99 Planets: any damage source that calls enemies.damage() directly (a power, a DoT, a split child killed by terrain) awards no gold, no score, no kill count. Either route all damage through TowerManager.applyDamage or claim the free enemies.onKill slot.
  ! ENEMY_TYPES is a module-level mutable const and every Enemy holds a live reference to its def (enemies.js:62). Mutating ENEMY_TYPES.mite.armor for a wave-3 evolution permanently mutates it for every enemy already alive, for every subsequent run in the same page load, and for the classic mode too. Evolutions must be per-enemy fields or a cloned def table, never in-place mutation.
  ! `nextId` is a module-level `let` (enemies.js:52) incremented in Enemy.init (61) and it is never reset - not even by clearAll. It also feeds gameplay: spawn() derives the space-mode altitude band from `e.id % 3` (enemies.js:200). A run replayed with the same seed after a previous run will therefore put enemies in different altitude bands. This must be moved into injected state for a deterministic pure core.
  ! Two raw Math.random() calls inside the spawn path: enemies.js:80 (`this.phase = Math.random() * Math.PI * 2`, cosmetic only) and enemies.js:194 (`_tmp.set(Math.random()-0.5, ...)` portal scatter, which perturbs e.dir and therefore the whole trajectory - NOT cosmetic). waves.js:124 adds a third (`Math.random() * 0.3` spawn jitter) and towers.js:742 a fourth. A seeded-RNG core must displace at least enemies.js:194 and waves.js:124.
  ! `this._render(dt)` is called with an argument at enemies.js:335 but `_render()` at 436 declares no parameter; it uses `this.time` (accumulated at 260) instead. Harmless today, but do not assume _render receives dt.
  ! EnemyManager.clearAll (enemies.js:489-491) has zero callers in js/. A roguelite that restarts a planet must call it explicitly, and must also reset waves.victoryFired (waves.js:157) and game state - there is no existing full-reset path.
  ! Spawn silently returns null at the cap (enemies.js:189, CONFIG.limits.maxEnemies = 220 at config.js:137). Splitting mites plus boss escorts (which already spawn 2 mites per shed plate, up to 5 sheds = 10 mites) can hit that ceiling on a 15-wave run and the loss is invisible. The InstancedMesh buffers are sized to the same constant at 172, so raising it costs GPU memory per species part.
  ! Boss escort spawning happens INSIDE damage() (enemies.js:223-224) while towers.js is mid-iteration over this.enemies.active (towers.js:795/835 forward loops, towers.js:535). Appends during a forward loop mean freshly spawned children can be targeted by the same tower in the same tick. A split-on-death power inherits this.
  ! `e.alt` is set in spawn (199-201) and NOT in Enemy.init, so a pooled enemy that reaches spawn via a path that skips 199 would carry the previous occupant's alt. towers.js:672 defends with `(e.alt ?? e.type.altitude)` but enemies.js:326 and 449 read e.alt bare.
  ! spaceMode is captured once in the constructor (enemies.js:95, `CONFIG.map.mode === 'space'`) and changes the leak test (317 vs 325), the separation plane test (288) and the bob (448). If 99 Planets ships different map modes per planet, EnemyManager must be rebuilt per planet or spaceMode made settable.
  ! armour has a hard floor: `dmg = Math.max(1, amount - armor)` (enemies.js:212). An armour-stacking evolution can never fully block chip damage, and a tower firing many small hits (helios ticks, tesla) is disproportionately good against it. Also `armorPierce: 99` at towers.js:840 (mortar) makes armour evolutions invisible to mortars.
  ! enemies.js:4 imports `{ R, terrainHeight }` from './world.js' and calls terrainHeight in the hot loop (enemies.js:87, 312) and R in five places (291, 310, 319, 326, 449). Any pure js/run/ core cannot own movement without either duplicating the terrain query or receiving R and a height function by injection.
  ! damage() returns the post-mitigation `dmg`, which towers.js:708 accumulates into tower.damageDealt and towers.js:711 renders as a floater. If a shield absorbs the hit, decide deliberately whether the return value counts absorbed damage - the tower stats and the damage numbers both read it.
  ~ Callback slots, not imports. EnemyManager declares nullable hooks in its constructor (enemies.js:99-102: onLeak, onKill, onDeathFx, onSpawnFx) plus two undeclared ones assigned from outside (onShedFx used at 222, onLandFx used at 379 via `this.onLandFx?.(e)`). Guard style is `if (this.onX) this.onX(e);` everywhere except 379 which uses optional chaining. A 99-Planets shell should subscribe to these rather than edit enemies.js internals - this is the file's existing extension seam and it already matches 'emit events rather than call the renderer'.
  ~ Module-level scratch vectors, allocation-free hot loop. enemies.js:34-50 declares 17 shared THREE temporaries (_dir, _fwd, _des, _sep, _tmp, _up, _m4, _q, _s, _mBasis, _col, _look, _frame, _lp, _lq, _ls, _eul). Nothing inside update/_render/_placePart allocates. Any added code in these paths must reuse or add module-level scratch, never `new THREE.Vector3()` per frame.
  ~ Object pooling with an active list. Enemies live in this.pool / this.active (97-98); spawn does `this.pool.pop() || new Enemy()` (191) and _release (252-257) splices and returns to pool. Enemy.init (60-88) re-initialises EVERY field because instances are recycled - any new per-enemy field (shield, armorBonus, evolution tier, splitDepth) MUST be reset in init() or it leaks across the pool into the next run.
  ~ Backwards iteration when the collection can shrink. update() at 263: `for (let i = this.active.length - 1; i >= 0; i--)`. towers.js and game.js iterate this.enemies.active forwards (towers.js:535, 795, 835; game.js:361) - those are read-only passes except towers.js:707 which can kill mid-loop.
  ~ Cosmetic state is derived, never stored in the sim. The header comment at enemies.js:6-9 states it explicitly, and _render/_placePart read only (time, e.phase, e.flashT, e.slowFrac, e.spawnT, e.plates, e.hopPrev). Keep evolution tells as derived reads of a sim field, not a second animation state.
  ~ Instanced rendering with a per-frame count reset. _render zeroes part._n (441), fills, then writes part.mesh.count = part._n and needsUpdate (480-486). Buffers are sized CONFIG.limits.maxEnemies * p.per at construction (172), so a mode that raises the enemy cap must raise it before EnemyManager is constructed.
  ~ Config-driven constants. Caps, economy and palette all come from CONFIG/PALETTE (config.js). enemies.js hardcodes a few tuning numbers inline though: 1.12 brittle (214), 0.09 flash (216), 6 boss plates (82/219), 2.1 heart radius (261), 3.4 space leak radius (319), turn 2.6/3.4 (304), separation 1.25/34 (291/300).
  ~ Every enemy holds a REFERENCE to the shared def: `this.type = type` (enemies.js:62) where type is ENEMY_TYPES[typeKey] (190). Reads go through e.type.X. Per-enemy overrides therefore need new e.* fields plus an edited read site, following how e.speed (69) already shadows type.speed.

### waves.js
  ! THE BUNDLER CANNOT SEE SUBDIRECTORIES. tools/build.mjs:13-16 hardcodes a FLAT module list `const GAME_MODULES = ['noise','config','audio','postfx','camera','world','nav','effects','towers','enemies','waves','ui','game','main']` and rewrites imports with `src.replace(/from\s+'\.\/([\w-]+)\.js'/g, "from '$1'")` at build.mjs:36. `[\w-]+` does not match `/`, so `from './run/core.js'` and `from '../run/core.js'` are left verbatim, are absent from the generated import map, and resolve to nothing inside a data: URI module — dist/worldheart.html and dist/artifact.html silently break. Any js/run/ and js/modes/ files REQUIRE editing GAME_MODULES and the rewrite regex, or the shipped build dies while the dev server still works.
  ! js/ IS DUPLICATED INTO v2/js/. `diff -q` confirms js/main.js, js/waves.js and js/config.js are byte-identical to v2/js/*. v2/ has its own index.html, css, lib. Recent commits ('Refresh /v2 with ...') show the two are hand-synced. Any waves.js or main.js edit must be mirrored into v2/js/ or /v2 regresses.
  ! GLOBAL Math.random() IN THE SIM PATH, three sites, all reachable from waves: waves.js:124 spawn-time jitter `t: 1.2 + i * g.gap + Math.random() * 0.3`; enemies.js:194 portal scatter `_tmp.set(Math.random()-0.5, ...)`; enemies.js:80 `this.phase = Math.random() * Math.PI * 2` (cosmetic but on the sim object). Also nav.js uses a seeded mulberry32 (nav.js:169) while world.js:51 uses `mulberry32(seed ^ 0x50ACE)`. A seeded-RNG-by-injection core cannot claim determinism while the spawn queue it drives is jittered by the global RNG — these must be routed through the injected RNG or the core must own the queue.
  ! waveComp(this.wave) IS CALLED TWICE per wave start: waves.js:110 (`const comp = waveComp(this.wave)`) for the queue, and waves.js:133 (`this.onWaveStart(this.wave, waveComp(this.wave))`) for the callback payload. Identical today because it is pure; a comp that reads a mutating modifier or consumes RNG will hand the HUD a different composition than the one that actually spawns.
  ! THREE FIELDS ARE READ BEFORE THEY ARE EVER DECLARED. `this.onSpawnPortal` (read waves.js:149, never in the constructor, assigned only at main.js:290); `this.victoryFired` (read waves.js:156 while undefined); `this.clock` (read waves.js:144, assigned only at waves.js:131). Harmless today, but a mode that calls update() before _startWave() ever ran will do `undefined += dt` -> NaN clock. Same pattern in Game: `this.onGameEnd` is read at game.js:260 but not initialised alongside onHudChange/onToast/onLeakFx at game.js:202-204.
  ! THERE IS NO WAY TO PAUSE BETWEEN WAVES. The clear block unconditionally sets `this.state = 'countdown'; this.countdown = CONFIG.waves.prepTime;` at waves.js:160-161. A draft-1-of-3 modal must either set `waves.state = 'idle'` from the shell (safe: waves.js:137 and :143 both fall through on 'idle') or stop calling waves.update, which means duplicating the sim gate at main.js:396-402.
  ! THE '30 WAVES' AND 'BOSS EVERY 10' ASSUMPTIONS ARE HARDCODED IN FIVE PLACES OUTSIDE waves.js: ui.js:370 `const boss = n % 10 === 0` (a wave-15 boss never gets its banner or 'boss' audio, wave 10 wrongly does); ui.js:407 the literal string 'thirty waves repelled'; ui.js:551 and ui.js:556 render `WAVE n/${CONFIG.waves.count}` and switch to 'ENDLESS n' past count; waves.js:29 `const boss = w % 10 === 0` inside waveComp; waves.js:156 the victory gate `this.wave >= CONFIG.waves.count`. CONFIG.waves.count is 30 (config.js:122) and is GLOBAL, not per-map — a 15-wave mode changing it changes the classic modes too unless the shell overrides these read sites.
  ! THE VICTORY GATE IS >= AND LATCHED, SO ENDLESS PLAY CONTINUES AFTER VICTORY. waves.js:156-159 fires onVictory once (victoryFired) but the director keeps starting waves forever; ui.js:416 shows a 'continue' button for a win and ui.js:357-361 unpauses. A 15-wave run must stop the director explicitly.
  ! THE PORTAL COUNT IS DRIVEN BY THE MAP, NOT BY THE MODE. portalCount (waves.js:15-20) reads CONFIG.map.portalWakes; activePortals (waves.js:99-101) slices nav.portalNodes to it; nav sizes the site search from `this.portalTarget = CONFIG.map.portalWakes.length` (nav.js:122); and the visual sync closure at main.js:295-298 mirrors it onto world.portals[i].active. A 15-wave mode on the default 'pocket' map ([1,4,9,14], config.js:31) gets its fourth breach on wave 14, one wave before the boss.
  ! THE ENEMY CAP SILENTLY DROPS SPAWNS. enemies.js:189 `if (this.active.length >= CONFIG.limits.maxEnemies) return null;` with maxEnemies = 220 (config.js:137). WaveDirector decrements pendingSpawns at waves.js:148 regardless of the null return, so a dropped spawn is invisible; and because the wave-clear predicate is `enemies.active.length === 0` (waves.js:152), dropped spawns make a wave clear EARLY rather than hang. Splitting enemies (the wave-12 evolution) push directly against this cap — the existing boss shed at enemies.js:218-226 already spawns two mites per plate through the same capped path.
  ! ENEMY STATS ARE READ OFF THE SHARED TYPE LITERAL, NOT OFF THE ENEMY. `e.type.armor` (enemies.js:211), `e.type.bounty`/`e.type.score` (game.js:235-236), `e.type.damage` (game.js:253), `e.type.boss` (enemies.js:218, game.js:245). ENEMY_TYPES (enemies.js:11-32) is a plain exported object and towers.js:66-69 already sets the precedent of MUTATING a type literal at module scope for the space map. Wave-3/6/9/12 evolutions must NOT follow that precedent — mutating ENEMY_TYPES persists across runs in the same page session (the only reset is a full reload via ui._reboot, ui.js:350).
  ! THE SIM STEP IS SCALED AND CLAMPED. main.js:326 clamps raw frame delta to `Math.min(raw, 1/30)`; main.js:397 multiplies by game.speed (game.js:178, cycled by HUD.cycleSpeed at ui.js:326). So waves.update receives simDt, not wall time — a pure core taking dt must be fed simDt from inside the `if (simDt > 0)` block at main.js:398-402, or its wave clock will diverge from the director's.
  ! stepFrame IS ALSO CALLED BY THE CAMERA HARNESS. camTest's `settle()` at main.js:483 runs `stepFrame(1/60, false)` hundreds of times, and camTest is invoked by WH.camTest (main.js:799). If a run core is stepped inside stepFrame, camTest will advance the run as a side effect — camTest already saves/restores rig and game.buildType (main.js:454-458, 764-768) but nothing else, so the core needs its own guard or a save/restore in the harness.
  ! world.addPortal is BOOT-ONLY AND UNPAIRED. main.js:248-253 builds world.portals from nav.portalNodes once; world.js:1599-1602 pushes and there is no remove. A roguelite that changes the frontier or the breach set between waves cannot re-run that loop without leaking meshes.
  ! waves.begin() (waves.js:85-89) resets only wave/state/countdown. It does NOT clear queues, pendingSpawns, clock or victoryFired, and it does not touch game.gold/lives/kills/score (set once in the Game constructor, game.js:173-176) or towerMgr.towers. There is no in-page restart path: ui.js:347-356 reboots by writing localStorage and calling this._reboot(). A 99 Planets run loop that restarts without a reload has to write that reset itself.
  ~ Callback CHAINING, not assignment. main.js:299-300 is the established idiom and the only example in the repo: `const prevStart = waves.onWaveStart; waves.onWaveStart = (n, comp) => { syncPortals(); prevStart?.(n, comp); };`. HUD._wire (ui.js:369-380) has already claimed onWaveStart, onWaveClear and onVictory by the time boot reaches line 284, so js/modes/ninetynine.js must wrap all three the same way and must be installed AFTER main.js:276.
  ~ Callbacks are bare nullable properties named `onX`, checked with `if (this.onX)` or `?.` at the fire site, never an event emitter. WaveDirector declares five in its constructor (waves.js:78-82) and reads a sixth undeclared one (onSpawnPortal, waves.js:149). Game does the same (game.js:202-204 plus the undeclared onGameEnd read at game.js:260). The pure run core's event emission should surface at the shell boundary in exactly this shape.
  ~ Zero build step, ES modules, flat `./name.js` imports only. Every import in js/ is either `'three'` (import map, index.html:27) or `'./<name>.js'`. There is no subdirectory anywhere under js/ today — js/run/ and js/modes/ do not exist (verified).
  ~ Module-scope singletons carrying mutable state that is read live: world.js:32 `export const BATTLEFIELD = { center: null, theta: 0 }` mutated by setBattlefield (world.js:33-36); world.js:48 `export let SPACE`; config.js:92 CONFIG (CONFIG.seed itself is mutated during seed search at nav.js:152). A run-modifier object fits this house style, but the 99 Planets brief says the core must import nothing — so the modifier object must live in the core's state and be handed to readers by the shell, not exported as a module singleton.
  ~ Cosmetic vs simulation separation is explicit and enforced by comment (enemies.js:8-9, towers.js:8-9, ui.js:5-7): 'Cosmetic animation reads sim state, never the other way around'. Renderer writes never feed back into sim. A pure core emitting events for the shell to render is the same contract, one level up.
  ~ Debug surface: every scripted-verification affordance hangs off window.WH (main.js:784-825) and advances via WH.step (main.js:801-805), which drives stepFrame with a fixed 1/60 and no rAF. Add 99 Planets probes there.
  ~ Fail-soft for optional subsystems, with a console.warn and a comment explaining the failure mode: main.js:118-145 setupLighting wraps PMREM and shadows in separate try/catch blocks; main.js:345-353 wraps stepFrame per frame; main.js:833-840 catches boot rejection and paints the message onto the boot screen.
  ~ Numbers live in CONFIG (config.js:92-140) or in the MAPS table (config.js:23-81), not inline. CONFIG.waves is {count:30, prepTime:22, firstPrep:30, earlyBonusPerSec:2} at config.js:121-126.

### style.css (394 lines)
  ! VERIFIED IN A LIVE BROWSER against the real rules: `.overlay.show` computes `pointer-events: none`, not auto. `#hud > *` (css/style.css:84) has specificity (1,0,0) and beats `.overlay.show` (0,2,0). So the `pointer-events: auto` written in .overlay.show at css/style.css:289 is dead code for the overlay element itself. The dim backdrop therefore does NOT swallow drags: with an overlay up, dragging the backdrop still rotates the camera. A draft overlay cannot rely on its backdrop for modality; it must pause the sim.
  ! VERIFIED: a `<button>` inside a HIDDEN overlay still computes `pointer-events: auto` (from css/style.css:85). Only `visibility: hidden` keeps it out of hit-testing - document.elementFromPoint over such a button returns the element behind it. This is exactly the invisible-map-card reset bug recorded in the code comment at css/style.css:283-285 and introduced-fixed in commit a4904e2. If a new overlay is written with opacity-only fading, its buttons become invisible-but-clickable and the map cards' `_reboot()` path (js/ui.js:213-218) is one stray click away from wiping the run.
  ! VERIFIED: a plain `<div>` inside a SHOWN overlay computes `pointer-events: none` and is unclickable. Draft cards built as divs will look right and do nothing.
  ! `.build-card.locked` (css/style.css:220) is purely cosmetic: no `pointer-events: none`, and the click handler at js/ui.js:197 stays bound. Clicking an unaffordable card still calls game.toggleBuild(key), which has no gold check (js/game.js:281-291); the denial only happens at placement time via _tryPlace -> 'Not enough gold' (js/game.js:393-395). Contrast `.btn:disabled` at css/style.css:129, which DOES set pointer-events:none. Wave-gated tower unlocks need an explicit guard, not just the class.
  ! No overlay has a z-index. #title-overlay and #end-overlay stack purely by DOM order inside #hud, so a draft overlay inserted before #end-overlay would be painted under the end screen.
  ! showEnd() sets `this.game.paused = true` (js/ui.js:418) but never calls reflectPause(), so the #btn-pause glyph is left showing the pause icon after a game ends. Do not copy that omission.
  ! togglePause() and cycleSpeed() early-return only on `this.game.state !== 'playing'` (js/ui.js:425, 432). The global keydown at js/ui.js:363-367 has no overlay guard, so Space still unpauses while an overlay is open unless game.state is changed or the handler is gated.
  ! js/main.js:404 calls `ui.update(dt)` but `update()` (js/ui.js:543) declares no parameter. Harmless today, but a new per-frame draft timer cannot assume dt is in scope there.
  ! There is no js/run/ and no js/modes/ directory. `ls -d js/*/` returns nothing; js/ is flat with 14 files. Both directories are new.
  ! tools/build.mjs cannot handle subdirectories: GAME_MODULES (lines 13-16) is a flat list of bare names joined as `js/<name>.js` (line 35), and the import rewrite regexes at lines 36-37 use `[\w-]+` which excludes '/'. Adding js/run/ and js/modes/ breaks dist/worldheart.html and dist/artifact.html silently unless both are updated.
  ! There are two published copies of the source to keep in sync: dist/worldheart.html and dist/artifact.html (regenerated by tools/build.mjs), and a full v2/ tree with its own css/, js/, lib/ and index.html. Recent commits ('Refresh /v2 with ...') show v2 is updated by hand or by copy.
  ! index.html:24 is `<div id="hud" aria-live="polite"></div>` - the ENTIRE HUD is an aria-live region. Injecting a draft overlay's text into it will be announced by screen readers along with every wave label and gold tick already in there. There is no focus management, no role="dialog", and no Escape handler on any overlay (Escape at js/game.js:227 only cancels build mode).
  ! The global `@media (prefers-reduced-motion: reduce)` block at css/style.css:385-393 forces `transition-duration: 0.01ms !important` on everything. That collapses the overlay's `visibility 0s linear 320ms` delay too, which is fine (visibility just flips immediately), but any new overlay logic must not depend on a transition actually taking 320ms.
  ! Dead or vestigial members in HUD: `this.thumbs = {}` (js/ui.js:38) is never read; `this._lastCountdownText = ''` (js/ui.js:40) is never read; line 384 in _waveTag (`const comp = this.waves.constructor === Object ? [] : [];`) always evaluates to [] and comp is unused. Do not model new code on them.
  ! js/ui.js line 1 is `import * as THREE from 'three'` and line 3 imports buildTowerVisual/MAT/TOWER_SCALE from towers.js, so ui.js is firmly on the renderer side of the line. The pure js/run/ core must never be imported by ui.js directly; route it through js/modes/ninetynine.js.
  ! Hard-coded 30-wave assumptions that a 15-wave mode will contradict: CONFIG.waves.count (js/config.js:122, value 30) is read four times in update() at js/ui.js:551 and 556, and the literal string 'thirty waves repelled' is in showEnd at js/ui.js:407. Lives fraction reads CONFIG.economy.startLives (js/ui.js:490; value 20 at js/config.js:130).
  ! The tower hotkeys are a hard-coded map in the sim, not the HUD: `const keys = { Digit1: 'bolt', Digit2: 'cryo', Digit3: 'mortar', Digit4: 'tesla', Digit5: 'helios' }` at js/game.js:225. The `.build-key` badge in _buildCards is only `i + 1` from the TOWER_TYPES key order, so the two will silently diverge if the key order changes.
  ! makeThumbnails() (js/ui.js:266-310) renders every TOWER_TYPES entry and writes into `this.cards[typeKey].querySelector('img')`. If a locked tower's card were omitted from _buildCards, this would throw on a null card at line 305. Keep all five cards and lock them visually instead.
  ! DESIGN.md line 65 lists a `.build-card` `disabled` variant and line 66 a `.pill` `alert` variant; neither `.build-card.disabled` nor `.pill.alert` exists in css/style.css. The real class is `.build-card.locked`. Do not code against the DESIGN.md names.
  ~ Overlay show/hide: ALWAYS `el.classList.add('show')` / `el.classList.remove('show')` on a `<div class="overlay">` that is a direct child of #hud. Never set style.display, style.opacity or style.visibility on an overlay by hand. The base .overlay rule (css/style.css:278-288) carries `visibility: hidden` and `transition: opacity 320ms var(--ease-out), visibility 0s linear 320ms`; .overlay.show (css/style.css:289) restores `visibility: visible` with a transition list that omits visibility so it flips instantly on entry and 320ms late on exit.
  ~ Any clickable thing inside an overlay must be a real `<button>` element (or carry class .panel or .build-card), because css/style.css:84 sets `#hud > * { pointer-events: none }` at ID specificity and only css/style.css:85 re-enables `#hud button, #hud .panel, #hud .build-card`. _buildMapCards (js/ui.js:201) is the reference: `const card = document.createElement('button')`.
  ~ Element lookup: never call document.getElementById at runtime. Add the id to the array literal at js/ui.js:166-175 and read it as `this.el['my-id']`. The three _build* helpers are the only places that use getElementById, and only during construction.
  ~ All markup lives in the single template literal in _build() (js/ui.js:49-160). Add new overlay markup there, after the #end-overlay block, before the closing backtick on line 160.
  ~ All event wiring lives in _wire() (js/ui.js:314-381). Add new listeners there, in the same `this.el['id'].addEventListener('click', () => ...)` form, and play `this.audio?.play('click')` on any confirm action.
  ~ Callbacks are attached as plain properties, not events: game.onHudChange / game.onToast / game.onLeakFx / game.onGameEnd are assigned at js/ui.js:316-319; waves.onWaveStart / onWaveClear / onVictory at js/ui.js:369-380; ui.onQuality is assigned from outside at js/main.js:278. To add behaviour without editing ui.js, chain the existing handler exactly as js/main.js:299-300 does: `const prev = waves.onWaveClear; waves.onWaveClear = (n, r) => { draft.open(n); prev?.(n, r); };`
  ~ State display is pull-based: refresh() (js/ui.js:484) re-reads the whole game object and reconciles the DOM with classList.toggle. Follow that instead of pushing incremental DOM writes. Per-frame values go in update() (js/ui.js:543), which guards writes with `if (e['wave-label'].textContent !== label)` to avoid touching the DOM every frame.
  ~ Bars animate with transform: scaleX(frac) on a .bar-fill with transform-origin:left (js/ui.js:492, js/ui.js:572; css/style.css:166-170). Never animate width.
  ~ Restarting anything means persisting intent with storeLocal() then `location.href = location.pathname` via _reboot() (js/ui.js:261-263). Query strings are explicitly not trusted on every host.
  ~ Copy voice: mechanics first, flavour in italic `<em>` below (DESIGN.md line 87). No em dashes anywhere in shipped text or comments (DESIGN.md line 78). Tracked uppercase only for short system markers via class .marker (css/style.css:153).
  ~ Restarting a CSS keyframe requires the reflow flush pattern from banner() (js/ui.js:467-469): `b.classList.remove('show'); void b.offsetWidth; b.classList.add('show');`.

### config.js
  ! THE FRONTIER-GROWTH BLOCKER, measured not estimated: the nav graph cannot grow. nav._buildGraph (nav.js:229) reallocates dirs/pos/height/walk/block/dist/next/flow (nav.js:267-274) and rebuilds the keep/oldToNew index map (nav.js:245-256), so every node index changes. At Titan's Brow settings (R240, navDetail 9) a rebuild costs 148 ms at theta 0.12 rising to 2061 ms at theta 0.52 (11,567 -> 187,027 nodes). Fourteen inter-wave rebuilds sum to roughly 10 seconds of frozen main thread. At navDetail 7 the same sweep is 22 ms -> 182 ms, which is affordable. recomputeFlow() alone is only 7-20 ms at any size.
  ! A nav rebuild silently destroys live game state that nothing re-derives: this.block (all tower footprints, written by blockNodes at nav.js:713-719 keyed by tower.id from game.js:402), this.heartNode, this.portalNodes, and every active enemy's e.node (enemies.js:66, advanced at enemies.js:280). Also nav.js:695 caches a portal Set by array IDENTITY (`if (this._portalSetFor !== this.portalNodes)`), so reusing the same array object after a rebuild leaves validatePlacement checking stale node ids.
  ! The outside-the-cap haze is NOT a shader. faceColor (world.js:381-397) bakes it into the terrain's vertex-color attribute at buildTerrainMesh time (world.js:494-501) on a plain MeshStandardMaterial (world.js:537). Measured re-tint at terrainDetail 7: 327,680 faces x 6.6 us for slopeAt+terrainHeight = >= 2157 ms blocking. There is no uTheta anywhere on the terrain material. Either accept a frozen ring or port the haze to onBeforeCompile uniforms.
  ! The field wall, by contrast, is nearly free to regrow: SEG=200 is hard-coded and every buffer is SEG*2 regardless of theta (world.js:1404, 1414-1415), so vertex count, uvs and indices are identical at every theta. Measured full rebuild 0.88 ms; in-place position rewrite is cheaper still. But HEIGHT = clamp(R*theta*0.1, 3.4, 16) (world.js:1408) changes, so uniform uGround = BURY/(BURY+HEIGHT) must be updated with it or the hot ground seam floats.
  ! World has NO teardown. Grep for dispose in world.js returns only two internal geometry disposals (lines 525, 1043). addFieldWall/addCloudDeck (world.js:1587-1597) overwrite this.fieldWall/this.cloudDeck while leaving the previous mesh in the scene. Any recreate-per-wave strategy must scene.remove + geometry.dispose + material.dispose by hand.
  ! distMax does NOT track theta. camera.js:256-259 measures it against CONFIG.planetRadius * CAM_TUNE.maxAlt, deliberately (see the comment at camera.js:241-254). Only distMin tracks theta via camScale. At R240 with shipped defaults the frontier ramp moves distMin 2.54 -> 10.98 while distMax stays 112.8, so the usable zoom band SHRINKS by 8.4 units across the run and camera.js:654 re-clamps targetDist every frame - a player parked at maximum zoom-in will be silently pushed outward the instant theta rises.
  ! CONFIG.seed is mutated during worldgen: nav.js:152 `CONFIG.seed = (CONFIG.seed + 7919) >>> 0` inside the 14-attempt retry loop, followed by initTerrainField(CONFIG.seed). A pure js/run/ core seeded at boot from CONFIG.seed would be seeded from a value the world does not actually use. Read CONFIG.seed AFTER nav.build() returns.
  ! Five CONFIG.camera fields are dead: fov, distMin, distMax, distStart, rotSpeed. camera.js reads only near, far, latClamp, inertia, zoomDamp, shakeMax from that block - everything else comes from CAM_TUNE. Do not add camera tuning under CONFIG.camera expecting it to be read. MAPS.*.fineAbsolute is likewise dead in all four entries.
  ! config.js is NOT import-safe outside a browser. Line 4 reads location.search, line 7 localStorage, line 234 matchMedia at module scope. This is exactly why the js/run/ core must import nothing - it cannot import config.js either. Pass tuning in as plain data from js/modes/ninetynine.js.
  ! world.js:13 `export const R = CONFIG.planetRadius` is a frozen snapshot taken at import. So are the frequency constants at world.js:20-28 (FQ, FA, F_CONT, F_WARP, F_ROLL, F_MOIST, F_RIDGE, F_FINE, F_FINE2). Switching map or radius after modules load will not reach them.
  ! Decor is scattered only within BATTLEFIELD.theta * 1.5 (world.js:1146) at buildStep(4). A cap built at wave-1 theta 0.12 leaves everything past 0.18 rad bare, so a frontier that grows to 0.52 expands into empty ground unless scatter runs at the final theta.
  ! Portal separation scales with theta squared: nav.js:533 `const sepBase = capCenter ? Math.pow(capTheta * 0.68, 2) : 0.2;`. Choosing 5 breach sites inside a 0.12 rad cap gives sepBase 0.0067 - breaches will sit almost on top of each other and _chooseSites may fail outright (nav.js:553 `if (best < 0 || bestScore < sepMin) return false;`), forcing seed retries.
  ! rig.confine.center is used with acos(dot) and is never re-normalized (camera.js:663). It is cloned once from nav.fieldCenter at main.js:229. If the shell ever re-aims the field it must clone and normalize.
  ! waves.js:156 gates victory on CONFIG.waves.count, which ships as 30. A 15-wave mode must set it (or bypass WaveDirector). waveComp (waves.js:27) is hard-coded around `w % 5` and `w % 10 === 0` bosses at 10/20/30 - a 3-phase boss on wave 15 has no existing hook; wave 15 currently hits `case 0` (w%5===0) and is not a boss at all.
  ! The repo contains a full duplicate of js/ under v2/js/ (identical line numbers for every file checked: config.js, camera.js, nav.js, world.js, main.js). The recent commits ('Refresh /v2 with ...') show v2 is a deployment mirror that is hand-synced. Any change to js/ will need the same change in v2/js/ or the two will drift.
  ! The measured numbers above are Node 24 on this Windows machine, single-threaded, warm JIT. Browser figures will differ, but the ratios (navDetail 9 is ~11x navDetail 7; theta 0.52 is ~14x theta 0.12) are structural - node count scales as sin^2(theta) x 4^detail.
  ~ Live getters over cached values. camera.js:226 camScale re-reads CONFIG.map.fieldTheta on every access; waves.js:16 portalCount re-reads CONFIG.map.portalWakes on every call. A growing frontier should mutate CONFIG.map.fieldTheta and let these follow, rather than threading theta through call chains.
  ~ Shader uniform for anything that must animate; baked vertex attribute only for what is fixed. buildCloudDeck (world.js:654) puts uCenter/uTheta in uniforms and resizes for free; faceColor (world.js:381) bakes the same test into vertex colors and cannot. Copy the cloud-deck pattern for the terrain haze.
  ~ Long explanatory comments above any number that reverses an earlier decision. config.js:241-247, 254-263, 270-279 and camera.js:241-254 all record the measurement that overturned a prior value. CAM_RANGES carries an explicit 'Do not correct these toward earlier values without asking' (config.js:242-244).
  ~ Flat typed arrays indexed by node id, never objects per node (nav.js:267-274). A pure js/run/ core keeping plain serialisable data should follow the same shape: parallel arrays or plain object literals, no class instances.
  ~ Hook fields assigned from outside rather than events or an emitter. WaveDirector exposes onWaveStart / onWaveClear / onPortalWake / onVictory / onCountdown / onSpawnPortal as null fields (waves.js:78-82) and main.js wraps rather than replaces: main.js:299-300 `const prevStart = waves.onWaveStart; waves.onWaveStart = (n, comp) => { syncPortals(); prevStart?.(n, comp); };`. The 99 Planets shell should wrap the same way.
  ~ Deterministic seeded RNG by injection: mulberry32(CONFIG.seed ^ <tag>) with a distinct hex tag per subsystem - 0xDECAF (world.js:1505), 0xC0FFEE (world.js:481), 0xF00D (nav.js:487), 0xCA97 (nav.js:169), 0x50ACE (world.js:51), 0x9e3779b9/0x51ab3c/0x7f4a7c15/0x2545f4 (world.js:124-128). The pure core should take an already-constructed RNG function, matching mulberry32's () => number contract in js/noise.js.
  ~ Module-scope scratch vectors reused across calls to avoid per-frame allocation (camera.js:5-22, world.js _t1/_t2/_pos/_m4, nav.js _v/_v2). Note nav.js:415 _scratch() uses a generation-marked Int32Array instead of a fresh visited set precisely because worldgen runs it per node on 160k+ graphs.
  ~ Guard every localStorage access in try/catch - config.js:6-11 is the only pattern used, and the comment names private mode as the reason.
  ~ Costs are stated as measurements in comments, not guesses (world.js:1515-1518 '327,680 triangles', config.js:178-184 'dark pixels went 639 at 0.55 to 1,414 at 0'). Any perf claim in the 99 Planets plan should carry the same kind of measured number.```
