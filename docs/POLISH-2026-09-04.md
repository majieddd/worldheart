# Polish pass, 2026-09-04

Owner brief: "the sword swing looks terrible, and the animation does not even
show when you're in 3rd person; you can't jump; the movement feels clunky and
weird with no head bobbing. Massively improve the game experience in all
aspects, fix any graphical artifacts or polygon issues, and make the
animations of the characters and mobs and their actual body/build really good,
detailed, but also polygon style."

This file is the tracked list for that request. Every row was measured on the
running game before it was written down, and every row is closed by a
measurement, not by a diff.

## What was measured before any change (seed 12345, 99 Planets, Bulwark)

| # | Finding | Evidence |
|---|---|---|
| F1 | The first-person swing barely moves. The blade is a 15%-scale prop in the bottom-right corner with no arm; at a third of the way through an 0.85 s swing the tip has moved a few pixels. | Screenshots at p=0.14 and p=0.33 with `game.speed = 0.08` |
| F2 | In third person the commander holds NO weapon: the sword exists only in the first-person view model. The swing is two box arms sliding on a 0.28-unit arc. | `allies.js` species table has no weapon part; TP screenshot mid-swing shows nothing |
| F3 | Jump works on F, but Space, the key every player presses, PAUSES the game while possessed. The hop is 1.27 units with no camera anticipation or landing dip, so even F reads as nothing. | keydown KeyF: hop 1.18 at 0.3 s, `game.speed` unchanged; keydown Space: `togglePause` in `ui.js:485` |
| F4 | Movement snaps to full speed on the first frame and stops on the last. The only head bob is a 0.035-unit vertical sine, no lateral sway, no roll, no landing, no footsteps. | `allies.driveUnit` has no velocity; `possess.placeCamera` bob term |
| F5 | The commander body is a box torso, box arms, box legs, box head, cone crest, flat cape. Front view confirms. | Inspect-camera screenshots, front and side |
| F6 | Mobs are primitives: mite = octahedron plus six cones, husk = octahedron chain, aegis = cylinder plus two slabs, wisp = octahedron plus two tetra wings, colossus = a glowing sphere with six orbiting boxes. | `enemies.js` species table |
| F7 | Enemy melee has NO animation: `windT` is written by the sim and never read by `_placePart`, so an enemy hits a unit with no wind-up, no lunge and no visible tell. | `grep -n windT js/enemies.js` |
| F8 | Five combat callbacks are declared and never assigned: `onShotFired`, `onBeam`, `onShellBurst` (allies) and `onMeleeWindUp`, `onMeleeHit` (enemies). The marksman's shot has no tracer, the oracle's beam is invisible, the bombardier's shell is invisible in flight and on burst, and enemy strikes have no FX. | `grep -rn "onShotFired\|onBeam\|onShellBurst\|onMeleeWindUp\|onMeleeHit" js/` matches only the declarations |
| F9 | `EVO_TIERS[].tint` is authored for every evolution tier and read by nothing, so the "visual tell" the README promises for each evolution does not exist. | `grep -n tint js/enemies.js` |
| F10 | Melee damage lands at the START of the swing, before any visible arc, so the hit and the gesture never coincide. | `allies.playerAttack` resolves hits synchronously |

## Plan and status

| # | Item | Files | Status |
|---|---|---|---|
| P1 | `js/rig.js`: procedural skeleton (parent-first joints, world matrices), low-poly geometry helpers (slab, wedge, cone, merge, mirrorX), animation curves | rig.js | done |
| P2 | Ally bodies rebuilt on the rig: pelvis/spine/chest/neck/head, shoulder/elbow/hand, hip/knee/foot, per-archetype proportions, helmets with visor, pauldrons, gauntlets, boots, belt, cape segments | allies.js | |
| P3 | Ally animation: idle breath and weight shift, walk/run cycle with knee bend and counter-swing, jump tuck and landing squash, hurt flinch, per-kind strike (cleave, twin cuts, rifle shoulder and recoil, overhand lob, two-hand beam brace) | allies.js | |
| P4 | Third-person weapons in hand, per archetype, matching the first-person props | allies.js | |
| P5 | Melee hit resolves at the strike frame of the swing, not at the click; hit stop on a landed blow | allies.js, game.js, main.js, possess.js | |
| P6 | First-person view model: arms and gauntlets on the grip, a real swing with anticipation, snap, follow-through and recovery, alternating direction, a blade trail ribbon, camera roll on the swing | viewmodel.js, possess.js | |
| P7 | Movement: acceleration and deceleration, sprint on Shift with a lens kick, reduced air control, figure-eight head bob scaled by speed, strafe lean, landing dip, footstep audio at the bob troughs | possess.js, allies.js, audio.js | |
| P8 | Jump on Space while possessed; pause moves to P (Space still pauses on the board); F kept as an alias; HUD key hints and README updated | possess.js, ui.js, README.md | |
| P9 | Mobs rebuilt on the rig with articulated bodies and gaits, melee wind-up telegraph and lunge, hurt flinch, evolution tint applied | enemies.js | delegated |
| P10 | Wire the dead FX hooks: tracer for the shot, a visible beam, a visible shell and burst, enemy wind-up glow and hit spark | main.js, effects.js | |
| P11 | Third-person camera: position smoothing, turn lag, shoulder side follows strafe | possess.js | |
| P12 | Verify: `node tools/test.mjs` green, `WH.camTest()` green on all five maps, screenshots of every item above, a full possessed wave played, `node tools/deploy.mjs`, push | | |
| P13 | Plugin: a worldheart reference in aegis-gamedev with the repo location, the run commands, the invariants and what this pass measured | aegis-suite | |
